Write an implementation plan for the spec agreed for $TICKET_KEY.

Assume the engineer executing it has no context for this codebase. Bite-sized steps, real code in every step, tests before implementation, a commit per task.
