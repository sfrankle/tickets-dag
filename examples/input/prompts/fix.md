Fix exactly one review finding in this working tree.

Finding{where}:
{summary}

{body}

Make the smallest change that genuinely addresses it.
Do not fix anything else, and do not tidy code you happen to pass.
If the finding is already satisfied by what is in the tree, change nothing and say so.

Do not commit; the caller commits, with the trailer that closes the finding.
